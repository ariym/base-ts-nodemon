{
  "name": "node-ffmpeg",
  "version": "1.0.0",
  "description": "extract mp3 or wav from mp4 returned by url",
  "main": "index.js",
  "scripts": {
    "start": "echo RUNNING npm run dev && npm run dev",
    "dev": "NODE_ENV='development' nodemon --watch './**/*.ts' --exec 'ts-node' index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "init": "echo INSTALLING AND THEN RUNNING IN DEV && npm i && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.9.3",
    "ts-node": "^10.9.1",
    "nodemon": "^2.0.20"
  }
}
